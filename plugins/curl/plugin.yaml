name: curl
description: Manage curl HTTP client settings
author: VIDYANKSHINI
version: 1.0.0
type: python
main: src/plugin.py
capabilities:
  - config_provider

install_info:
  default_manager: 'scoop'
  packages:
    winget: 'Anantshri.curl'
    scoop: 'curl'
    choco: 'curl'
