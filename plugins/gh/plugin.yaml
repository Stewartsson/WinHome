name: gh
version: 1.0.0
type: python
main: src/plugin.py
capabilities:
  - config_provider

install_info:
  default_manager: 'scoop'
  packages:
    winget: 'GitHub.cli'
    scoop: 'gh'
    choco: 'gh'
